{
  "buildCommand": "npm run build",
  "installCommand": "rm -rf node_modules .next && npm ci",
  "framework": "nextjs",
  "regions": ["fra1"],
  "build": {
    "env": {
      "PRISMA_ENGINES_CHECKSUM_IGNORE_MISSING": "1"
    }
  }
}
